[package]
name = "fastdb"
version = "0.1.22"
edition = "2024"
publish = false
description = "Safe Rust projection of the FastDB portable-payload Core"
license = "MIT"
repository = "https://github.com/world-in-progress/fastdb"
readme = "README.md"

[dependencies]
fastdb-sys = { path = "../fastdb-sys" }
