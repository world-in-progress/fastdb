{
  "name": "fastdb4ts",
  "version": "0.0.0",
  "private": true,
  "description": "TypeScript bindings for fastdb running in the browser via WebAssembly.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src/wasm"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && mkdir -p dist/wasm && cp src/wasm/*.js src/wasm/*.d.ts dist/wasm/ && if ls src/wasm/*.wasm >/dev/null 2>&1; then cp src/wasm/*.wasm dist/wasm/; fi",
    "build:wasm": "bash ../build-wasm.sh",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.8.2"
  }
}
