name: Release fastdb-core

on:
  push:
    tags:
      - 'core/v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout default branch
        uses: actions/checkout@v6
        with:
          ref: ${{ github.event.repository.default_branch }}
          fetch-depth: 0

      - name: Extract changelog section
        id: changelog
        run: |
          SECTION=$(awk '/<!-- BEGIN:fastdb-core -->/,/<!-- END:fastdb-core -->/' CHANGELOG.md \
            | sed '1d;$d')
          echo "SECTION<<EOF" >> $GITHUB_OUTPUT
          echo "$SECTION" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: fastdb C++ core ${{ github.ref_name }}
          body: ${{ steps.changelog.outputs.SECTION }}

      - name: Reset changelog section
        run: |
          python3 - <<'PYEOF'
          import re, pathlib
          path = pathlib.Path("CHANGELOG.md")
          content = path.read_text()
          placeholder = (
              "## fastdb C++ core\n\n"
              "Wait and hope for the best...\n"
          )
          content = re.sub(
              r"(?<=<!-- BEGIN:fastdb-core -->\n).*?(?=<!-- END:fastdb-core -->)",
              placeholder,
              content,
              flags=re.DOTALL
          )
          path.write_text(content)
          PYEOF

      - name: Commit and push cleared changelog
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add CHANGELOG.md
          git commit -m "chore: reset fastdb-core changelog after ${{ github.ref_name }} release"
          git push
